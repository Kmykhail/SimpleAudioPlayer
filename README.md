# SimpleAudioPlayer
Only wav format :(
please install SDL:</br>

sudo apt-get install libsdl2-dev</br>

sudo apt-get install libsdl2-ttf-dev</br>

mkdir build</br>
cd build </br>
cmake ..</br>
make</br>
run
